import 'package:flutter/material.dart';

class NavigateArrow extends StatelessWidget {
  const NavigateArrow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.arrow_forward_ios, size: 13);
  }
}
