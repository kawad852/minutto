import '../../shared.dart';

class PaginationHandler extends StatelessWidget {
  const PaginationHandler({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
